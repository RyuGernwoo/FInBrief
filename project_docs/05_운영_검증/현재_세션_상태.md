# Current Session State

> **Session ID**: S-20260714-001
> **Status**: Langfuse LLMOps 고도화 구현 및 검증 완료
> **Last completed session**: S-20260713-003 LiteLLM Guardrail/Fallback 설정

## Dashboard

| Item | Value |
|------|-------|
| Current Gate | Gate E 기록 중 |
| Gate Progress | deterministic eval, eval_runs 저장, Langfuse score fail-open 전송, RAG/delivery span 보강 |
| Start Date | 2026-07-08 |
| Next Action | 커밋 및 PR 설명서 작성 |

## Current Planning Summary

| Item | Value |
|------|-------|
| Session ID | S-20260714-001 |
| Goal | Langfuse LLMOps 고도화 기획서의 P0 범위인 자동 평가·score·저장·span 세분화를 구현 |
| Source Of Truth | `project_docs/04_구현_기획/Langfuse_LLMOps_고도화_기획.md` (Gate A) |
| Scope | `app/core/evaluations.py`, `app/core/langfuse_scores.py`, `app/agents/pipeline.py`, `app/agents/nodes.py`, repository eval 저장소, Supabase schema, 테스트/운영 문서 |
| Excluded | Langfuse dataset/experiment 자동화, prompt registry 운영, 실제 Langfuse UI smoke |
| Required User Action | Supabase SQL 재실행 후 `eval_runs` 신규 컬럼 확인, Langfuse UI에서 score 표시 smoke |

## Current Implementation Evidence - S-20260714-001 (2026-07-14)

| Item | Result |
|------|--------|
| Deterministic eval | `app/core/evaluations.py`: disclaimer, forbidden terms, RAG evidence coverage, numeric consistency, card schema 평가 |
| Langfuse score | `app/core/langfuse_scores.py`: trace_id 기반 score 전송, metadata redaction, disabled/unavailable fail-open |
| Pipeline integration | `run_morning_pipeline()`이 eval_results 생성, repository 저장, score 전송 count, API `eval_summary`를 연결 |
| Repository/schema | memory/Supabase evaluation repository 추가, `eval_runs`에 `trace_id`, `run_date`, `topic_id`와 index 추가 |
| Span detail | `retrieve_evidence`, `deliver` 노드에 RAG/delivery count metadata span 추가 |
| Verification | 신규 평가/score 5 passed, 관련 경로 13 passed / 1 warning, `python -m compileall app` 통과, CI 조건 env 지정 전체 163 passed / 1 warning |

## Current Implementation Evidence - S-20260713-003 (2026-07-13)

| Item | Result |
|------|--------|
| RED tests | `tests/test_settings.py`, `tests/test_llm_guardrails.py`, `tests/test_llm_fallback.py` 추가 후 9개 실패로 현재 미구현 요구 확인 |
| Guardrail module | `app/core/llm_guardrails.py`: PII/webhook/API key masking, card JSON 필수 key 검사, 금융 금칙어 차단 |
| LiteLLM wrapper | `chat_json()`이 Settings 기반 timeout/retry/fallback, metadata, guardrail profile, schema error 변환을 처리 |
| LangGraph fallback | `_analyze()`가 LLM/guardrail 예외 시 `_local_analysis()`로 복구, 이미지 프롬프트도 guardrail profile 적용 |
| Proxy example | `config/litellm_config.yaml.example`에 선택적 LiteLLM Proxy guardrail/fallback 예시 추가 |
| Verification | `python -m compileall app` 통과, `python -m pytest -p no:cacheprovider --basetemp .pytest_cache\basetemp-llm-policy-full --disable-warnings` 112 passed / 1 warning |

## Pending From S-20260713-002

| Item | Result |
|------|--------|
| Docker check | Docker Desktop 또는 GCE 환경에서 `docker build -t finbrief:multi-stage -f Dockerfile .` 및 health smoke 재확인 필요 |

## Current Implementation Evidence - S-20260713-002 (2026-07-13)

| Item | Result |
|------|--------|
| Dockerfile | `builder` stage에서 wheelhouse 생성, `runtime` stage에서 `/wheels` 설치, non-root user/healthcheck/report path 유지 |
| Contract test | `tests/test_ci_cd_contracts.py`: `AS builder`, `AS runtime`, `COPY --from=builder`, `--find-links=/wheels` 검증 추가 |
| README | Docker 실행 섹션과 현재 검증 결과를 multi-stage 기준으로 갱신 |
| Verification | `python -m compileall app` 통과, `tests/test_ci_cd_contracts.py` 7 passed, 전체 pytest 104 passed / 1 warning, `git diff --check` 통과 |
| Docker check | `docker compose config` 통과. `docker build`는 Docker Desktop Linux Engine 미실행으로 미검증 |

## Current Implementation Evidence - S-20260710-005 (2026-07-13)

| Item | Result |
|------|--------|
| Observability helper | `app/core/observability.py`: Langfuse disabled/no-key no-op, env alias mapping, metadata sanitize, report trace context |
| LiteLLM metadata | `chat_json()`에 metadata 전달 및 `langfuse_otel` callback 활성화 경로 추가 |
| Report trace | `run_morning_pipeline()`이 report run 단위 trace_id를 state/API 응답에 연결 |
| Deployment env | `.env.example`, `.github/workflows/cd.yml`, `GitHub_설정.md`에 Langfuse SDK/OTEL 변수 반영 |
| Tests | 신규 `tests/test_observability.py`, LLM metadata/report trace/CI-CD 계약 테스트 추가 |
| Verification | `python -m pytest -p no:cacheprovider --basetemp .pytest_cache\basetemp-langfuse --disable-warnings` 104 passed / 1 warning, `python -m compileall app` 통과, workflow YAML parse ok |

## Current Implementation Evidence - S-20260711-001 (2026-07-11)

| Item | Result |
|------|--------|
| CI workflow | `FinBrief CI`: Python 3.11, mock/stub env, `compileall`, 전체 pytest, Docker build, summary job 구성 |
| CD workflow | `FinBrief CD`: CI 성공 후 main deploy, 수동 deploy/rollback, GHCR build/push, GCE SSH deploy, `/api/v1/health`, `.current_image/.previous_image` rollback 구성 |
| Docker runtime | `Dockerfile`, `.dockerignore`, `docker-compose.yml`: Python 3.11 slim, Noto CJK font, non-root user, report volume, healthcheck |
| README | Docker 실행, CI/CD workflow, GitHub Secrets, 최신 검증 명령 반영 |
| 운영 문서 | `GitHub_설정.md`, `GCP_서버_구축.md`, `CI_CD_문제해결.md` 추가 |
| Test contract | `tests/test_ci_cd_contracts.py`: workflow/Docker/README 계약 5건 |
| Verification | CI/CD contract 5 passed, `python -m compileall app` 통과, workflow YAML parse ok, `docker compose config` 통과, 전체 pytest 96 passed / 1 warning, Docker build 통과, 컨테이너 health `200 status=ok mock_data=true` |

## Current Implementation Evidence - S-20260710-004 (2026-07-10)

| Item | Result |
|------|--------|
| 지표 catalog | `app/agents/report_catalog.py`: 21개 주요 지표 slot, 표시명, icon_key, source alias 정의 |
| 리포트 renderer | `app/agents/report_render.py`: 1080x1080 PNG, 헤더, 3열 x 7행 지표판, 상승/하락/보합/누락 표시 |
| LangGraph 연결 | `collect_indicators -> build_report_image -> load_cached_cards` 순서로 전체 리포트 이미지를 fan-out 전에 1회 생성 |
| API 응답 | `/reports/run`, `/reports/today` summary에 `report_url` 포함 |
| Delivery 정합 보완 | `nodes.deliver`가 `discord_channel_id`를 읽어 Discord bot 전송 경로로 라우팅 |
| Verification | `python -m compileall app` 통과, `python -m pytest -p no:cacheprovider --basetemp .pytest_cache\basetemp-report-image --disable-warnings` 91 passed / 1 warning |

## Current Implementation Evidence - S-20260710-003 (2026-07-10)

| Item | Result |
|------|--------|
| 선택 토픽 service | `app/services/topic_ingestion.py`: FRED/yfinance/ECOS 지표 수집, RSS 필터/태깅, Upstage passage embedding row 생성, Supabase upsert 호출 |
| Ingestion API | `POST /api/v1/topics/{topic_id}/ingest` 추가. 존재하지 않는 토픽 404, mock mode 저장소 부재 시 409 |
| Report refresh | `/api/v1/reports/run`에 `refresh_data` 추가. active subscription topic을 선적재한 뒤 기존 LangGraph RAG 조회 경로 실행 |
| ECOS schema | `TopicSourceMapping`에 `statistic_code`, `cycle`, `item_code` optional 필드 추가 |
| Delivery 정합 | `nodes.deliver`가 `notifier.send_card()`를 호출하고 `dry_run` 상태를 schema에 포함 |
| Verification | `python -m compileall app` 통과, 핵심 묶음 26 passed, 전체 `python -m pytest -p no:cacheprovider --basetemp .pytest_cache\basetemp-ingest --disable-warnings` 78 passed / 1 warning |

## Current Implementation Evidence - S-20260710-002 (2026-07-10)

| Item | Result |
|------|--------|
| Provider 배선 | `app/api/dependencies.py`에서 supabase bundle에 `UpstageEmbeddingProvider().embed_query` 주입 → `news.match`(match_news RPC) 활성화 |
| RAG 후처리 | `app/agents/rag.py`: since(기본 3일)/유사도≥0.2/출처≤2/top-k=5 순수 헬퍼 |
| retrieve_evidence | fan-out 이전 캐시 미스 토픽에 RAG 근거+지표 선조회 주입, `build_card`는 payload만 소비 |
| 실수집 | live에서 `collect_indicators`(source_mapping FRED/yfinance), `ingest_news`(RSS→태깅→doc/embedding upsert) |
| live_data 게이트 | `ENABLE_MOCK_DATA=false`에서만 실데이터, 기본은 fixture → 기존 테스트 회귀 0 |
| Verification | `python -m pytest -p no:cacheprovider --basetemp=<writable>` 70 passed / 1 failed(`test_deliver_dry_run` 선재, RAG 무관·범위 외); 신규 `test_rag_retrieval` 9건 |

## Last Session Summary

| Item | Value |
|------|-------|
| Session ID | S-20260710-001 |
| Goal | 토픽 카탈로그 100+ 확장과 키워드-토픽 매칭 API |
| Verification | 전체 51 passed (당시 기준) |
